{{ '<?php' }}


namespace {{ event.namespace }}\Processor\Event;

use {{ event.abstract.namespace }};
use Codingbeard\Framework\Module\Service\Event\Message\CodingbeardServicePreOrderProcessEventMessage;

class {{ event.name }} extends {{ event.abstract.name }}

{
  /**
   * @devtoolsOverwritable
   *
   * @return string
   */
  public function getGroup()
  {
    return "{{ message.identifier.group }}";
  }

  /**
   * @devtoolsOverwritable
   *
   * @return string
   */
  public function getName()
  {
    return "{{ message.identifier.name }}";
  }

  /**
   * @devtoolsOverwritable
   *
   * @return CodingbeardServicePreOrderProcessEventMessage

   */
  public function getMessageClass()
  {
    return new CodingbeardServicePreOrderProcessEventMessage;
  }

  /**
   * This event will be called immediately before processing the order and submitting it to the billing provider.
   * It is the final point that propagation can be stopped before a user is charged for the service.
   *
   * @param CodingbeardServicePreOrderProcessEventMessage $message
   *
   * @return bool
   */
  public function process(CodingbeardServicePreOrderProcessEventMessage $message)
  {
    $this->log("Starting to process Event Message " . get_class($message));

    return true;
  }

}
